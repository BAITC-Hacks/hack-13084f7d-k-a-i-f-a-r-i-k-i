# Задачи команды (удалить перед сдачей)

Сейчас пайплайн работает сквозным на эвристиках: `python -m pipeline.run ...` → `python eval.py`
показывает **1 из 5** эталонных случаев и 3 ложных. Цель к 3:30 — 5 из 5 и 0 ложных.
UI уже работает на `data/mock_report.json` (там идеальный ответ — так должно выглядеть в финале).

Правило: каждый правит только свои файлы. Контракты — только через тимлида.

## Роль 1 - тимлид / LLM
- [ ] `extract.py`: extract_llm() по EXTRACT_PROMPT (эвристику оставить как fallback)
- [ ] `match_units.py`: читать приказ LLM-ом → merged / renamed / abolished / created с evidence на пункт приказа
- [ ] `match_functions.py`: эмбеддинги + LLM-судья для серой зоны.
      Ловушка из baseline: «реестр договоров» лексически похож на «реестр рисков» → rapidfuzz считает partial
- [ ] `report.py`: build_summary_llm() только по verified findings

## Роль 2 - данные
- [ ] `parse.py`: таблицы в Word, многоуровневая нумерация, пункты «1)» / «а)»
- [ ] второй тестовый комплект в другом стиле (нумерация, таблицы, «грязный» PDF) + ground_truth
- [ ] `eval.py`: прогон по всем комплектам, итоговая таблица для слайда
- [ ] как только организаторы дадут данные — прогнать и показать результат роли 1

## Роль 3 - дубли, конфликты, верификатор
- [ ] `llm/nvidia_client.py`: проверить модель эмбеддингов, заполнить .env
- [ ] `duplicates.py`: пары функций разных подразделений «после» + LLM-проверка (ловушка: анализ рынка сбыта в КД)
- [ ] `conflicts.py`: INCOMPATIBLE + сравнение с «до» (конфликт ПОЯВИЛСЯ после реорганизации — severity high)
- [ ] `verifier.py` уже работает — покрыть тестами кейсы с «ё», переносами, кавычками

## Роль 4 - UI, README, демо
- [ ] `app.py`: экспорт DOCX, подсветка цитат, фильтр по подразделению
- [ ] README: скриншоты, актуальные цифры eval
- [ ] слайды + видео-страховка на mock_report.json
