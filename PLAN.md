# Пошаговый план реализации OrgDiff Agent

Это рабочий список выполнения. Спецификация моделей и алгоритмов — в
[AGENTS.md](AGENTS.md), схема системы — в [docs/architecture.md](docs/architecture.md).
После выполнения каждого пункта его статус меняется на **[Done]** только при
зелёных тестах и выполненном критерии приёмки. Непроверенная часть остаётся
**[Todo]**, текущая работа — **[In progress]**. Следующие сессии продолжают с
первого незакрытого этапа и обновляют этот файл.

**Запуск всего сервиса — через Docker Compose.** На T1 контейнер выполняет
проверку конфигурации и автономные тесты. Работающий веб-сервис на порту `8501`
появится после T13 и интеграции T15; до этого полноценный запуск приложения
не считается готовым. Инструкция: [docs/docker.md](docs/docker.md).

## Порядок этапов

| Шаг | Статус | Что реализовать | Когда ставим [Done] |
|---|---|---|---|
| T0 | [Done] | Каркас, зависимости, `.env.example`, конфигурация, CLI, pytest/ruff, demo | CLI help и check работают; исходные 42 теста зелёные |
| T1 | [Done] | Все предметные Pydantic-модели и JSON-хранилище | Все модели проходят round-trip; атомарность, версия и безопасные пути проверены; 156 тестов и Ruff прошли локально и в Docker |
| T2 | [Todo] | Парсеры DOCX/PDF/XLSX, классификация, пункты, общие селекторы функций | Acceptance T2 и синтетические документы: автонумерация, таблицы, многостраничность, OCR, перестановка колонок |
| T3 | [Todo] | Единый LLM-клиент: structured output, эмбеддинги, кэш, ретраи, usage | Мок API подтверждает схему, повтор из кэша, обработку ошибок и неверных ID; секреты не сохраняются |
| T4 | [Todo] | Подразделения, действия приказов, согласование | Статусы соответствуют приказу; дополнительные NEW_EDITION не затирают структурные действия; неоднозначности видны человеку |
| T5 | [Todo] | Извлечение функций, атомизация, действие/объект, домен и роль | Составные функции разделены; каждый атом ссылается на исходный пункт и сохраняет точную цитату |
| T6 | [Todo] | Векторный поиск и судья покрытия | Все функции «до» имеют mapping; проверены потери, частичное покрытие и переносы между подразделениями |
| T7 | [Todo] | Дубли и пересечения ответственности | Найдены оба целевых дубля; ловушка разных объектов не срабатывает; новизна рассчитывается после coverage |
| T8 | [Todo] | Конфликты интересов и независимость контроля | Основной конфликт найден с HIGH; проверены бонусный случай и отрицательные примеры |
| T9 | [Todo] | Проверка evidence и ссылок | Несуществующий источник отбрасывается; цитата и атрибуты источника восстанавливаются; неполные данные не выдаются за доказательство |
| T10 | [Todo] | AnalysisReport, заключение, MD и DOCX | Экспорты читаемы, все ссылки `[ID]` валидны; ошибочный текст повторяется ограниченно, затем используется шаблон |
| T11 | [Todo] | LangGraph, SQLite, HITL, полный CLI | End-to-end CLI завершается; каждый этап пишет артефакт; resume без дублей, редактирование и инвалидация зависимых результатов проверены |
| T12 | [Todo] | Eval и цикл настройки промптов/порогов | Потери 2/2, дубли 2/2, конфликт 1/1, trap_hit=0, FP≤1; результаты воспроизводимо записаны |
| T13 | [Todo] | Streamlit: загрузка, подразделения, функции, отклонения, заключение | Браузерный сценарий проходит от загрузки через подтверждение до DOCX; порт `8501` доступен через Docker |
| T14 | [Todo] | Чат поверх готового run | Ответы используют только инструменты результатов и содержат ссылки; недостаток данных обозначен явно |
| T15 | [Todo] | Полный запуск через Docker Compose | `docker compose up --build` поднимает UI и весь пайплайн; demo работает; OCR-языки установлены; данные переживают перезапуск |
| T16 | [Todo] | Финальный README, схема, скриншоты, результаты, демо | Новый человек запускает сервис через Docker по инструкции; все критерии сдачи проверены |
| T17 | [Todo, опционально] | Нормативка, бенчмаркинг, рекомендации | Выполнять после обязательных этапов, с отдельными тестами и проверкой доказательств |

## Выполненные подшаги T1

1. [Done] Модели: `Fragment`, `Document`, `Unit`, `OrderAction`, `UnitChange`,
   `Function`, `FunctionMapping`, `Evidence`, `Finding`, `AnalysisReport`, enum.
2. [Done] `ArtifactStore`: типизированные save/load, UTF-8, атомарная
   замена файла, `manifest.json` с версией, защита путей.
3. [Done] Проверки round-trip, границ confidence, независимости defaults,
   повреждённого JSON, несовместимой версии, ошибок записи и выхода за каталог.
4. [Done] Читаемые имена всех 20 документов в `HackAlem_test_dataset`.
   SHA-256 всех 22 файлов совпадают с `data/demo`, ссылки эталона — 27/27.
   История переименований: [eval/dataset_renames.json](eval/dataset_renames.json).
5. [Done] Основа Docker: runtime-проверка настроек, отдельный контейнер
   тестов, mounts конфигурации и volumes прогонов/кэша. Это ранняя подготовка
   инфраструктуры; T15 остаётся открытым до готовности сервиса.
6. [Done] Общая локальная и контейнерная проверка: **156 passed** в обеих средах,
   Ruff и форматирование проходят, Docker check завершается с кодом `0`.

## Правила закрытия этапов

- После изменения кода запускать соответствующие тесты и общий `pytest -q`,
  проверять `ruff check .` и `ruff format --check .`.
- Контейнерные зависимости расширять по мере появления импортов новых этапов;
  полный runtime должен содержать `pipeline`, `ui` и OCR.
- LLM-тесты запускать явно с `--run-llm`; mock-проверки не заменяют acceptance,
  для которого спецификация требует реальный API.
- Отметка [Done] означает реализацию и проверку, а не наличие файла-заглушки.
- Каждый этап вести в отдельной ветке. До настройки Git remote изменения
  фиксируются локально; удалённый PR не считается созданным.
- Не включать эталонные ответы в production-алгоритмы. Исходный и тестовый
  комплект могут переименовываться только по явному запросу пользователя;
  содержимое документов при восстановлении имён сохраняется.

## Журнал выполнения

| Этап | Результат |
|---|---|
| T0 | [Done] Каркас и архитектура; 42 теста, Ruff и CLI прошли. Коммит `f640489`, ветка `codex/t0-foundation` |
| T1 | [Done] Модели, хранилище, нормальные имена исходников; 156 тестов локально и в Docker, Ruff и контейнерная проверка конфигурации прошли; ветка `codex/t1-models-storage` |

Следующий этап после T1 — **T2: загрузка документов и извлечение фрагментов**.
